# Get number of available CPU threads

Returns the number of CPU threads available for parallel processing.

## Usage

``` r
get_threads(default = 1L)
```

## Arguments

- default:

  Integer. Default value if detection fails (default: 1).

## Value

Integer number of threads.
